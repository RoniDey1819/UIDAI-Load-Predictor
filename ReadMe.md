# UIDAI Load Predictor

This project aims to predict the load for UIDAI services using historical data.

## Project Structure

- `data/`: Contains raw, processed, and feature data.
- `pipelines/`: Data ingestion, cleaning, and aggregation pipelines.
- `models/`: Forecasting models.
- `config/`: Configuration settings.
